# credentials.yml
rest:
  # No necesita configuración adicional para REST básico

# Si quieres usar SocketIO (opcional)
socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: false